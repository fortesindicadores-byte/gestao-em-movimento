name: Abast Painel Inspect

# O que a erp_abastecimentos tem para um painel de litros e valor: mês a mês,
# filiais, projetos e buracos. Só totais no log. Não grava nada.
on:
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - name: Inspecionar
        env:
          GEM_SUPABASE_SERVICE_KEY: ${{ secrets.GEM_SUPABASE_SERVICE_KEY }}
        run: node scripts/abast-painel-inspect.mjs
